spring.application.name=polygen_research
server.port=1010

# --- BASE DE DATOS (Dinámica) ---
# Si existe la variable de entorno (Docker), la usa. Si no, usa el valor por defecto (Local).
spring.datasource.url=${SPRING_DATASOURCE_URL:jdbc:mysql://localhost:3306/polygen_research?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true}
spring.datasource.username=${SPRING_DATASOURCE_USERNAME:root}
spring.datasource.password=${SPRING_DATASOURCE_PASSWORD:}
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect

# --- GMAIL (Dinámico) ---
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=${MAIL_USERNAME:polygenresearchsys@gmail.com}
spring.mail.password=${MAIL_PASSWORD:birj lojp nxdk cnrx}

spring.mail.properties.mail.smtp.auth=true 
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.connectiontimeout=5000
spring.mail.properties.mail.smtp.timeout=5000
spring.mail.properties.mail.smtp.writetimeout=5000